#ifndef ILLEGAL_RACER_SHADER_H
#define ILLEGAL_RACER_SHADER_H

namespace sh {
    class Shader {
    private:
        const char *const TAG = "Shader";

    public:
        Shader();

        ~Shader();
    };
}

#endif
